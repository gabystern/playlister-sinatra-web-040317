<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/songs/<%=@song.id%>" method="POST"
      <h4>Song Name</h4>
      <input type="text" name="song[name]" id="song_name" placeholder="Song Name"/>

      <h4>Genres</h4>
      <% @genres.each do |genre| %>
          <p><%= genre.name %></p>
          <input type="checkbox" name="song[genre_ids][]" value="<%=genre.id%>" id="<%=genre.name%>" >
          <%end%>
      <h4>Artist</h4>
      <input type="text" name="song[artist_id]" placeholder="Artist Name">

      <input type="submit" name="Submit">
    </form>
  </body>
</html>
