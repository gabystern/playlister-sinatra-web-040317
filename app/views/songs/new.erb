<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create a New Song and Artist</title>
  </head>
  <body>
    <form action="/songs" method=POST>
      <label>Song Name: </label>
      <input type="text" name="song[name]" id="Name"</input>
      <label>Artist Name: </label>
      <input type="text" name="song[artist]" id="Artist Name"</input>

      <% @genres.each do |genre| %>
      <br />
        <input id="<%= genre.name %>" type="checkbox" name="genres[]" value="<%= genre.id %>"><%=genre.name%></input>
      <% end %>

      <br />

      <input type="submit" value="Create" </input>
    </form>
  </body>
</html>
